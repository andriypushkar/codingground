using System.IO;
using System;

class Parser
{

    static public Node Parse(string statement)
    {
        Console.WriteLine(statement);
        Stack stack = new Stack();
        Node root = new Node();
        
        
        
        return null;
    }
    
}